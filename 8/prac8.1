#include<iostream>
#include<string>
#include<fstream>
using namespace std;

int main()
{
	string s;
	ifstream fin("test.txt");
	while (fin >> s)
		cout << s << endl;
	fin.close();
	system("pause");
	return 0;
}
