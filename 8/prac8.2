#include<iostream>
#include<string>
#include<fstream>
using namespace std;

int main()
{
	string s;
	cout << "Input text" << endl;
	getline(cin,s);
	ofstream fout("test.txt");
	fout << s;
	fout.close();
	system("notepad.exe test.txt");
	return 0;
}
