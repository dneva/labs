#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double a,b,n,m;
    cout << "a,b" << endl;
    cin >> a >> b;
    if(a>=b)
        n=pow(a-b,1/3);
        else if(a<b)
            n=a*a+(a-b)/sin(a*b);
    if(n<b)
        m=(n+a)/-b+pow((sin(a)*sin(a)-cos(n)),1/2);
        else if(n==b)
        m=b*b+tan(n*a);
        else
        m=pow(b,3)+n*a*a;
    cout << "m = " << m << endl << "n = " << n;
    return 0;
}
